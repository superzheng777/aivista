"use client";

import { type InfiniteData, useInfiniteQuery, useMutation, useQueryClient } from "@tanstack/react-query";
import { CheckCircle2, Clipboard, Download, Ellipsis, FolderClock, Heart, ImageOff, LoaderCircle, MessageSquare, PencilLine, Send, Sparkles, Trash2 } from "lucide-react";
import Link from "next/link";
import { useRouter, useSearchParams } from "next/navigation";
import { useEffect, useRef, useState } from "react";
import { needsImageUrlRefresh, type GenerationAsset, type GenerationSession, type GenerationTask, type GenerationTurn } from "@/entities/generation/model/generation";
import { ImageDetailShell } from "@/entities/generation/ui/image-detail-shell";
import { assetQueryKeys, deleteGenerationAssets, getGenerationAsset, setGenerationImageFavorites } from "@/features/assets/api/asset-api";
import { downloadOriginalGenerationImage } from "@/features/assets/lib/original-image-download";
import { generationQueryKeys, listGenerationSessions, listGenerationTurns } from "@/features/generation/api/generation-api";
import { GenerationComposer } from "@/features/generation/ui/generation-composer";
import { useGenerationEventStream, type GenerationSessionIndicator } from "@/features/generation/model/generation-event-stream-provider";
import { mergeGenerationTurnPageData, type GenerationTurnPage } from "@/features/generation/model/generation-turn-cache";
import { PublicationFormDialog } from "@/features/publication/ui/publication-form-dialog";
import { cn } from "@/lib/utils";
import { AccentSquare, DotMatrix } from "@/shared/ui/editorial-ornaments/editorial-ornaments";

function taskStatusText(task: Pick<GenerationTask, "status" | "retryCount" | "maxRetryCount">): string {
  const retryProgress = `${task.retryCount}/${task.maxRetryCount}`;
  if (task.status === "QUEUED" && task.retryCount > 0) return `模型调用失败，正在重试（${retryProgress}）`;
  if (task.status === "QUEUED") return "正在生成图片";
  if (task.status === "SUCCEEDED") return "生成已完成";
  if (task.status === "PARTIALLY_SUCCEEDED") return "部分图片已生成";
  if (task.status === "FAILED") return "生成失败";
  return "尚未开始生成";
}

export function GenerateWorkspace() {
  const queryClient = useQueryClient();
  const router = useRouter();
  const searchParams = useSearchParams();
  const sessionId = searchParams.get("sessionId");
  const { sessionIndicators, syncVersion } = useGenerationEventStream();
  const sessionsQuery = useInfiniteQuery({ queryKey: generationQueryKeys.sessions(), queryFn: ({ pageParam }) => listGenerationSessions(pageParam), initialPageParam: undefined as string | undefined, getNextPageParam: (lastPage) => lastPage.nextCursor ?? undefined });
  const sessions = sessionsQuery.data?.pages.flatMap((page) => page.items);
  const activeSessionTitle = sessionId ? sessions?.find((session) => session.id === sessionId)?.title : undefined;
  useEffect(() => { if (syncVersion > 0) void queryClient.refetchQueries({ queryKey: generationQueryKeys.sessions(), type: "active" }); }, [queryClient, syncVersion]);
  function selectSession(nextSessionId: string): void { router.push(`/generate?sessionId=${encodeURIComponent(nextSessionId)}`); }

  return <section className="min-h-dvh overflow-x-hidden bg-[var(--page-bg)] text-[var(--primary)]"><div className="grid min-h-dvh lg:h-dvh lg:grid-cols-[320px_minmax(0,1fr)]">
    <aside className="hidden min-h-0 border-r border-[var(--border)] bg-[var(--surface-bg)] lg:block"><div className="sticky top-0 flex h-dvh flex-col overflow-y-auto px-6 py-[30px]"><p className="pl-1 text-[11px] font-semibold leading-none tracking-[0.16em] text-[var(--text-secondary)]"><span className="text-[var(--accent)]">02</span> / CREATE</p><div className="mt-[14px] flex items-center justify-between"><h1 className="text-2xl font-bold leading-tight">开启创作</h1><Sparkles className="size-5 text-[var(--text-secondary)]" /></div><button type="button" onClick={() => router.push("/generate")} className="mt-6 inline-flex h-[52px] w-full items-center gap-3 rounded-[7px] bg-[var(--primary)] pl-7 text-left text-base font-semibold text-[var(--surface-bg)] transition-colors hover:bg-[var(--primary-hover)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)] focus-visible:ring-offset-2"><PencilLine className="size-[18px]" />新对话</button><div className="mt-5"><SessionList sessions={sessions} indicators={sessionIndicators} isLoading={sessionsQuery.isLoading} isError={sessionsQuery.isError} hasNextPage={sessionsQuery.hasNextPage} isFetchingNextPage={sessionsQuery.isFetchingNextPage} activeSessionId={sessionId} onSelect={selectSession} onLoadMore={() => void sessionsQuery.fetchNextPage()} onRetry={() => void sessionsQuery.refetch()} /></div></div></aside>
    {sessionId ? <ConversationPanel sessionId={sessionId} sessionTitle={activeSessionTitle} /> : <NewConversationPanel />}
  </div></section>;
}

function NewConversationPanel() { return <main className="relative flex min-h-[calc(100dvh-4rem)] min-w-0 flex-col overflow-hidden bg-[var(--page-bg)] px-5 py-16 pb-24 sm:px-8 lg:min-h-dvh lg:px-12 lg:py-0"><WorkspaceDecorations /><div className="relative z-10 mx-auto flex w-full max-w-[1040px] flex-1 flex-col justify-center lg:-translate-y-5"><div className="text-center"><p className="text-[11px] font-semibold tracking-[0.16em] text-[var(--text-secondary)]"><span className="text-[var(--accent)]">01</span> / START CREATING</p><h1 className="mt-6 text-[28px] font-bold leading-[1.25] tracking-tight sm:text-[32px] lg:text-[38px]">你好，想创作什么？</h1><p className="mt-[14px] text-[15px] leading-6 text-[var(--text-secondary)]">写下第一个想法，提交后会自动建立创作会话。</p></div><div className="mt-10"><GenerationComposer /></div></div><ArchiveLine label="TURN AN IDEA INTO AN IMAGE" /></main>; }

function ConversationPanel({ sessionId, sessionTitle }: { sessionId: string; sessionTitle?: string }) {
  const queryClient = useQueryClient(); const router = useRouter(); const { acknowledgeSession, sessionIndicators, syncVersion } = useGenerationEventStream();
  const [isComposerCollapsed, setIsComposerCollapsed] = useState(false);
  const [detailAsset, setDetailAsset] = useState<GenerationAsset | null>(null);
  const [publishAsset, setPublishAsset] = useState<GenerationAsset | null>(null);
  const [actionNotice, setActionNotice] = useState<string | null>(null);
  const turnsQuery = useInfiniteQuery<GenerationTurnPage, Error, InfiniteData<GenerationTurnPage>, ReturnType<typeof generationQueryKeys.turns>, string | undefined>({ queryKey: generationQueryKeys.turns(sessionId), queryFn: ({ pageParam }) => listGenerationTurns(sessionId, pageParam), initialPageParam: undefined as string | undefined, getNextPageParam: (lastPage) => lastPage.nextBefore ?? undefined, structuralSharing: mergeGenerationTurnPageData });
  const turns = turnsQuery.data ? [...turnsQuery.data.pages].reverse().flatMap((page) => page.items) : undefined;
  useEffect(() => { if (syncVersion > 0) void queryClient.refetchQueries({ queryKey: generationQueryKeys.turns(sessionId), type: "active" }); }, [queryClient, sessionId, syncVersion]);
  const favoriteMutation = useMutation({
    mutationFn: ({ imageId, favorite }: { imageId: string; favorite: boolean }) => setGenerationImageFavorites([imageId], favorite),
    onSuccess: () => void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.turns(sessionId) })]),
    onError: () => setActionNotice("收藏状态更新失败，请重试。"),
  });
  const deleteMutation = useMutation({
    mutationFn: (imageId: string) => deleteGenerationAssets([imageId]),
    onSuccess: (_result, imageId) => { setDetailAsset((asset) => asset?.id === imageId ? null : asset); void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.turns(sessionId) })]); },
    onError: () => setActionNotice("删除失败，请重试。"),
  });
  useEffect(() => { if (sessionIndicators[sessionId] === "COMPLETED" || sessionIndicators[sessionId] === "ATTENTION") acknowledgeSession(sessionId); }, [acknowledgeSession, sessionId, sessionIndicators]);
  async function refreshAsset(imageId: string): Promise<GenerationAsset> {
    const refreshed = await getGenerationAsset(imageId);
    setDetailAsset((asset) => asset?.id === imageId ? refreshed : asset);
    await queryClient.refetchQueries({ queryKey: generationQueryKeys.turns(sessionId), type: "active" });
    return refreshed;
  }
  async function openAsset(asset: GenerationAsset): Promise<void> {
    try { setDetailAsset(needsImageUrlRefresh(asset.imageUrls.display) ? await refreshAsset(asset.id) : asset); }
    catch { setActionNotice("图片访问地址刷新失败，请稍后重试。 "); }
  }
  function requestPublish(asset: GenerationAsset): void {
    if (asset.publicationReviewStatus === "PENDING") return setActionNotice("该图片正在审核中。");
    if (asset.publicationReviewStatus === "APPROVED") {
      router.push(`/inspirations?imageId=${encodeURIComponent(asset.id)}`);
      return;
    }
    setPublishAsset(asset);
  }
  if (detailAsset) return <>{actionNotice ? <p role="status" className="m-4 rounded-[7px] border border-[var(--accent-border)] bg-[var(--accent-soft)] px-4 py-3 text-sm text-[var(--accent-hover)]">{actionNotice}</p> : null}<ImageDetailShell image={detailAsset} refreshImage={refreshAsset} allowCopy={detailAsset.publicationReviewStatus === "NONE"} onClose={() => setDetailAsset(null)} actions={<ConversationAssetActions asset={detailAsset} isFavoriteUpdating={favoriteMutation.isPending} isDeleting={deleteMutation.isPending} onFavorite={() => favoriteMutation.mutate({ imageId: detailAsset.id, favorite: !detailAsset.favorited })} onPublish={() => requestPublish(detailAsset)} onDelete={() => { if (window.confirm("确定删除这张图片？此操作无法撤销。")) deleteMutation.mutate(detailAsset.id); }} />} />{publishAsset ? <PublicationFormDialog asset={publishAsset} onClose={() => setPublishAsset(null)} onSuccess={(result) => { setPublishAsset(null); setDetailAsset((asset) => asset ? { ...asset, publicationReviewStatus: result.status } : asset); setActionNotice("图片已发布，正在审核。"); void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.turns(sessionId) })]); }} /> : null}</>;
  return <main className="relative flex min-h-[calc(100dvh-4rem)] min-w-0 flex-col bg-[var(--page-bg)] lg:h-dvh lg:min-h-0"><header className="flex min-h-[74px] items-center justify-between border-b border-[var(--border)] bg-[var(--surface-bg)]/80 px-5 sm:px-8"><h1 className="min-w-0 truncate text-base font-bold sm:text-lg">{sessionTitle ?? "创作会话"}</h1><div className="ml-4 flex shrink-0 items-center gap-3"><Link href="/assets" className="inline-flex h-9 items-center gap-1.5 rounded-[6px] border border-[var(--border)] bg-[var(--surface-bg)] px-3 text-sm font-medium transition hover:bg-[var(--active-bg)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]"><FolderClock className="size-4" />资产库</Link></div></header><section aria-label="当前会话历史" onScroll={(event) => setIsComposerCollapsed(event.currentTarget.scrollHeight - event.currentTarget.scrollTop - event.currentTarget.clientHeight > 160)} className="min-h-0 flex-1 overflow-y-auto px-5 py-7 pb-[224px] sm:px-8 lg:px-12"><div className="mx-auto max-w-[1040px]">{actionNotice ? <p role="status" className="mb-4 rounded-[7px] border border-[var(--accent-border)] bg-[var(--accent-soft)] px-4 py-3 text-sm text-[var(--accent-hover)]">{actionNotice}</p> : null}{turnsQuery.isLoading ? <HistorySkeleton /> : null}{turnsQuery.isError ? <div role="alert" className="border border-[var(--accent)]/30 bg-[var(--accent-soft)] px-4 py-3 text-sm text-[var(--accent-hover)]"><p>历史对话加载失败，请重试。</p><button type="button" onClick={() => void (turnsQuery.hasNextPage ? turnsQuery.fetchNextPage() : turnsQuery.refetch())} className="mt-1 font-medium underline">重试</button></div> : null}{turnsQuery.hasNextPage ? <div className="mb-5 flex justify-center"><button type="button" onClick={() => void turnsQuery.fetchNextPage()} disabled={turnsQuery.isFetchingNextPage} className="inline-flex min-h-10 items-center gap-2 rounded-[6px] border border-[var(--border-strong)] bg-[var(--surface-bg)] px-3 text-sm text-[var(--text-secondary)] disabled:opacity-60">{turnsQuery.isFetchingNextPage ? <LoaderCircle className="size-4 animate-spin" /> : null}加载更早的对话</button></div> : null}{turns?.map((turn) => <ConversationTurn key={turn.id} turn={turn} onOpenAsset={openAsset} onRefreshAsset={refreshAsset} onFavorite={(asset) => favoriteMutation.mutate({ imageId: asset.id, favorite: !asset.favorited })} onPublish={requestPublish} onDelete={(image) => { if (window.confirm("确定删除这张图片？此操作无法撤销。")) deleteMutation.mutate(image.id); }} />)}{!turnsQuery.isLoading && !turns?.length ? <div className="flex min-h-56 items-center justify-center"><p className="text-sm text-[var(--text-secondary)]">这个会话还没有可展示的历史内容。</p></div> : null}</div></section><div className="pointer-events-none absolute inset-x-0 bottom-0 z-10 px-5 pb-5 sm:px-8 lg:px-12"><div className="pointer-events-auto mx-auto max-w-[1040px]"><GenerationComposer sessionId={sessionId} compact={isComposerCollapsed} onExpand={() => setIsComposerCollapsed(false)} /></div></div>{publishAsset ? <PublicationFormDialog asset={publishAsset} onClose={() => setPublishAsset(null)} onSuccess={() => { setPublishAsset(null); setActionNotice("图片已发布，正在审核。"); void Promise.all([queryClient.invalidateQueries({ queryKey: assetQueryKeys.all }), queryClient.invalidateQueries({ queryKey: generationQueryKeys.turns(sessionId) })]); }} /> : null}</main>;
}

function SessionList({ sessions, indicators, isLoading, isError, hasNextPage, isFetchingNextPage, activeSessionId, onSelect, onLoadMore, onRetry }: { sessions: GenerationSession[] | undefined; indicators: Record<string, GenerationSessionIndicator>; isLoading: boolean; isError: boolean; hasNextPage: boolean; isFetchingNextPage: boolean; activeSessionId: string | null; onSelect: (sessionId: string) => void; onLoadMore: () => void; onRetry: () => void }) { return <div className="space-y-2">{isLoading ? <SessionSkeleton /> : null}{isError ? <div role="alert" className="px-2 py-3 text-sm text-[var(--accent-hover)]"><p>会话加载失败，请重试。</p><button type="button" onClick={onRetry} className="mt-1 font-medium underline">重试</button></div> : null}{sessions?.map((session) => <SessionListItem key={session.id} session={session} indicator={activeSessionId === session.id ? undefined : indicators[session.id]} active={activeSessionId === session.id} onSelect={onSelect} />)}{hasNextPage ? <button type="button" onClick={onLoadMore} disabled={isFetchingNextPage} className="inline-flex min-h-10 w-full items-center justify-center gap-2 rounded-[6px] px-3 text-sm text-[var(--text-secondary)] disabled:opacity-60">{isFetchingNextPage ? <LoaderCircle className="size-4 animate-spin" /> : null}加载更多会话</button> : null}{!isLoading && !sessions?.length ? <p className="px-2 py-2 text-xs leading-5 text-[var(--text-secondary)]">尚无历史会话。</p> : null}</div>; }

function SessionListItem({ session, indicator, active, onSelect }: { session: GenerationSession; indicator?: GenerationSessionIndicator; active: boolean; onSelect: (sessionId: string) => void }) { const statusIndicator = session.hasActiveTask ? <LoaderCircle aria-label="正在生成" className="size-3.5 shrink-0 animate-spin text-[var(--accent)]" /> : indicator === "ATTENTION" ? <span aria-label="生成失败" className="size-2 shrink-0 bg-[var(--accent-hover)]" /> : indicator === "COMPLETED" ? <CheckCircle2 aria-label="有新的生成结果" className="size-3.5 shrink-0 text-[var(--accent)]" /> : null; return <button type="button" onClick={() => onSelect(session.id)} className={cn("flex h-[52px] w-full items-center gap-[10px] rounded-[7px] px-[14px] text-left text-sm transition", active ? "border border-[var(--accent-border)] bg-[var(--active-bg)] font-medium text-[var(--primary)]" : "text-[var(--text-secondary)] hover:bg-[var(--surface-soft)] hover:text-[var(--primary)]")}><MessageSquare className="size-[18px] shrink-0" /><span className="min-w-0 flex-1 truncate">{session.title}</span>{statusIndicator}{active ? <span aria-label="当前会话" className="size-[9px] shrink-0 bg-[var(--accent)]" /> : null}</button>; }

function ConversationTurn({ turn, onOpenAsset, onRefreshAsset, onFavorite, onPublish, onDelete }: { turn: GenerationTurn; onOpenAsset: (asset: GenerationAsset) => Promise<void>; onRefreshAsset: (imageId: string) => Promise<GenerationAsset>; onFavorite: (asset: GenerationAsset) => void; onPublish: (asset: GenerationAsset) => void; onDelete: (asset: GenerationAsset) => void }) { return <article className="py-7 first:pt-0"><div className="flex justify-start" aria-label="用户消息"><div className="w-fit max-w-full rounded-[7px] border border-[var(--accent-border)] bg-[var(--active-bg)] px-4 py-3 text-[var(--primary)]"><p className="whitespace-pre-wrap break-words text-sm leading-7">{turn.userMessage.content}</p>{turn.normalGenerationRequest.negativePrompt ? <p className="mt-2 border-t border-[var(--accent-border)] pt-2 text-xs leading-5 text-[var(--text-secondary)]">负面提示词：{turn.normalGenerationRequest.negativePrompt}</p> : null}</div></div><div className="mt-5 flex justify-start" aria-label="AI 回复"><div className="w-full max-w-[700px] rounded-[10px] border border-[var(--border)] bg-[var(--surface-bg)] p-4 shadow-[0_2px_4px_rgb(43_35_25_/_3%)] sm:p-[18px]"><div className="flex items-center justify-between gap-3"><p className="inline-flex items-center gap-2 text-sm font-semibold text-[var(--accent-hover)]"><span className="size-[11px] bg-[var(--accent)]" />AiVista</p><span className="rounded-[5px] bg-[var(--accent-soft)] px-2 py-1 text-xs font-medium text-[var(--accent)]">{taskStatusText(turn.generation)}</span></div><TaskOutcome task={turn.generation} />{turn.generation.failureMessage ? <p role="alert" className="mt-2 text-xs leading-5 text-[var(--accent-hover)]">{turn.generation.failureMessage}</p> : null}{turn.generation.images.length ? <div className={cn("mt-4 grid gap-1.5", imageGridClassName(turn.generation.images.length))}>{turn.generation.images.map((image) => <GenerationImageCard key={image.id} image={image} onOpen={() => void onOpenAsset(image)} onRefresh={() => onRefreshAsset(image.id)} onFavorite={() => onFavorite(image)} onPublish={() => onPublish(image)} onDelete={() => onDelete(image)} />)}</div> : null}</div></div><ConversationArchiveLine /></article>; }
function imageGridClassName(imageCount: number): string { if (imageCount === 1) return "max-w-[420px] grid-cols-1"; if (imageCount === 2 || imageCount === 4) return "grid-cols-2"; return "grid-cols-3"; }
function GenerationImageCard({ image, onOpen, onRefresh, onFavorite, onPublish, onDelete }: { image: GenerationAsset; onOpen: () => void; onRefresh: () => Promise<GenerationAsset>; onFavorite: () => void; onPublish: () => void; onDelete: () => void }) {
  const [menuOpen, setMenuOpen] = useState(false);
  const [imageUnavailable, setImageUnavailable] = useState(false);
  const [actionError, setActionError] = useState<string | null>(null);
  const retryUsedRef = useRef(false);
  async function retryImage(): Promise<void> { if (retryUsedRef.current) return setImageUnavailable(true); retryUsedRef.current = true; try { await onRefresh(); } catch { setImageUnavailable(true); } }
  async function download(): Promise<void> { try { await downloadOriginalGenerationImage(image); } catch { setActionError("下载失败，请稍后重试。"); } }
  async function copy(): Promise<void> { try { let current = image; if (needsImageUrlRefresh(current.imageUrls.display)) current = await onRefresh(); const fetchDisplay = async (asset: GenerationAsset) => { const url = asset.imageUrls.display?.url; if (!url) throw new Error("missing display"); const response = await fetch(url, { mode: "cors", referrerPolicy: "no-referrer" }); if (!response.ok) throw new Error("image fetch failed"); return response.blob(); }; let blob: Blob; try { blob = await fetchDisplay(current); } catch { blob = await fetchDisplay(await onRefresh()); } if (!navigator.clipboard?.write || typeof ClipboardItem === "undefined") throw new Error("clipboard unavailable"); await navigator.clipboard.write([new ClipboardItem({ [blob.type || "image/webp"]: blob })]); } catch { setActionError("复制失败，请使用下载。"); } }
  if (!image.imageUrls.thumbnail) return <div role="status" className="flex aspect-square flex-col items-center justify-center gap-2 rounded-[6px] border border-dashed border-[var(--border-strong)] bg-[var(--surface-soft)] px-4 text-center text-xs text-[var(--text-secondary)]"><ImageOff className="size-5" />图片已从资产库删除</div>;
  return <div className="group relative overflow-visible rounded-[6px] border border-[var(--border)] bg-[var(--surface-bg)]">
    <button type="button" onClick={onOpen} className="block w-full overflow-hidden rounded-[5px] text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--accent)]">
      {imageUnavailable ? <span className="flex aspect-square items-center justify-center bg-[var(--surface-soft)] text-xs text-[var(--text-secondary)]">图片已从资产库删除</span> : <>
        {/* Private short-lived signed URLs must not be sent through an image optimizer. */}
        {/* eslint-disable-next-line @next/next/no-img-element */}
        <img src={image.imageUrls.thumbnail.url} alt="本次生成的图片" loading="lazy" decoding="async" referrerPolicy="no-referrer" onError={() => void retryImage()} className="aspect-square w-full object-cover transition duration-200 group-hover:scale-[1.015]" />
      </>}
    </button>
    <div className="absolute right-2 top-2 z-10 flex translate-y-1 items-center gap-1 rounded-[6px] border border-white/30 bg-[var(--primary)]/90 p-1 text-[var(--surface-bg)] opacity-0 shadow-lg transition duration-150 group-hover:translate-y-0 group-hover:opacity-100 focus-within:translate-y-0 focus-within:opacity-100"><button type="button" onClick={() => void download()} className="grid size-7 place-items-center rounded-[4px] hover:bg-white/15" aria-label="下载原图"><Download className="size-3.5" /></button><button type="button" onClick={() => void copy()} className="grid size-7 place-items-center rounded-[4px] hover:bg-white/15" aria-label="复制展示图"><Clipboard className="size-3.5" /></button><button type="button" onClick={() => setMenuOpen((open) => !open)} className="grid size-7 place-items-center rounded-[4px] hover:bg-white/15" aria-expanded={menuOpen} aria-label="更多图片操作"><Ellipsis className="size-4" /></button>{menuOpen ? <div className="absolute right-0 top-[calc(100%+6px)] w-28 overflow-hidden rounded-[6px] border border-[var(--border)] bg-[var(--surface-bg)] py-1 text-[var(--primary)] shadow-xl"><button type="button" onClick={() => { setMenuOpen(false); onFavorite(); }} className="flex w-full items-center gap-2 px-3 py-2 text-left text-xs hover:bg-[var(--surface-soft)]"><Heart className={cn("size-3.5", image.favorited && "fill-[var(--accent)] text-[var(--accent)]")} />{image.favorited ? "取消收藏" : "收藏"}</button><button type="button" onClick={() => { setMenuOpen(false); onPublish(); }} className="flex w-full items-center gap-2 px-3 py-2 text-left text-xs hover:bg-[var(--surface-soft)]"><Send className="size-3.5" />{image.publicationReviewStatus === "APPROVED" ? "查看发布" : "发布"}</button><button type="button" onClick={() => { setMenuOpen(false); onDelete(); }} className="flex w-full items-center gap-2 px-3 py-2 text-left text-xs text-[var(--accent-hover)] hover:bg-[var(--accent-soft)]"><Trash2 className="size-3.5" />删除</button></div> : null}</div>{actionError ? <p role="status" className="absolute inset-x-1 bottom-1 rounded-[4px] bg-[var(--primary)]/85 px-2 py-1 text-center text-[10px] text-white">{actionError}</p> : null}
  </div>;
}
function ConversationAssetActions({ asset, isFavoriteUpdating, isDeleting, onFavorite, onPublish, onDelete }: { asset: GenerationAsset; isFavoriteUpdating: boolean; isDeleting: boolean; onFavorite: () => void; onPublish: () => void; onDelete: () => void }) { return <section><p className="text-xs font-medium tracking-wide text-muted-foreground">作品操作</p><div className="mt-3 grid grid-cols-2 gap-2"><button type="button" onClick={onFavorite} disabled={isFavoriteUpdating} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-border text-sm font-medium hover:bg-muted disabled:opacity-50"><Heart className={cn("size-4", asset.favorited && "fill-current text-[var(--accent)]")} />{asset.favorited ? "已收藏" : "收藏"}</button><button type="button" onClick={onPublish} className="inline-flex h-10 items-center justify-center gap-2 rounded-lg border border-border text-sm font-medium hover:bg-muted"><Send className="size-4" />{asset.publicationReviewStatus === "APPROVED" ? "查看发布" : "发布"}</button></div><button type="button" onClick={onDelete} disabled={isDeleting} className="mt-2 inline-flex h-10 w-full items-center justify-center gap-2 rounded-lg bg-destructive/10 text-sm font-medium text-destructive hover:bg-destructive/20 disabled:opacity-50"><Trash2 className="size-4" />删除图片</button></section>; }
function TaskOutcome({ task }: { task: GenerationTask }) { const isTerminal = task.status === "SUCCEEDED" || task.status === "PARTIALLY_SUCCEEDED" || task.status === "FAILED"; return isTerminal ? <p className="mt-2 text-xs leading-5 text-[var(--text-secondary)]">结果：成功 {task.completedImageCount} 张 · 失败 {task.failedImageCount} 张</p> : null; }
function WorkspaceDecorations() { return <div aria-hidden="true" className="pointer-events-none absolute inset-0 select-none"><span className="absolute right-[7%] top-[94px] hidden h-[390px] w-[290px] bg-[var(--active-bg)] lg:block" /><div className="absolute left-[13%] top-[150px] hidden h-[57px] w-[109px] lg:block"><DotMatrix columns={5} rows={5} dotSize={3} gap={7} opacity={0.55} className="absolute left-0 top-0" /><AccentSquare size={17} className="absolute bottom-0 right-0" /></div></div>; }
function ArchiveLine({ label }: { label: string }) { return <div aria-hidden="true" className="relative z-0 mt-auto hidden items-center gap-[14px] pb-[58px] pt-8 lg:flex"><span className="size-2 shrink-0 bg-[var(--text-secondary)]" /><span className="h-px flex-1 bg-[var(--border-strong)]" /><span className="shrink-0 whitespace-nowrap text-[10px] font-medium tracking-[0.18em] text-[var(--text-secondary)]">{label}</span><span className="size-2 shrink-0 bg-[var(--accent)]" /></div>; }
function ConversationArchiveLine() { return <div aria-hidden="true" className="my-6 flex items-center gap-3"><span className="h-px flex-1 bg-[var(--border-strong)]" /><span className="whitespace-nowrap text-[10px] font-medium tracking-[0.18em] text-[var(--text-secondary)]">CREATED FROM YOUR IDEA</span><span className="size-2 bg-[var(--accent)]" /><span className="h-px flex-1 bg-[var(--border-strong)]" /></div>; }
function HistorySkeleton() { return <div className="space-y-6 animate-pulse"><div className="h-5 w-4/5 bg-[var(--skeleton)]" /><div className="h-64 max-w-xl rounded-[10px] bg-[var(--skeleton)]" /></div>; }
function SessionSkeleton() { return <div className="h-[52px] animate-pulse rounded-[7px] bg-[var(--skeleton-light)]" />; }
